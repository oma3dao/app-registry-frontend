# ============================================
# NETWORK CONFIGURATION
# ============================================
# Select active chain: 'localhost', 'omachain-testnet', or 'omachain-mainnet'
NEXT_PUBLIC_ACTIVE_CHAIN=localhost

# ============================================
# THIRDWEB CONFIGURATION
# ============================================
# Get your client ID from: https://thirdweb.com/dashboard
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=your_client_id_here

# Server-side secret key for DID verification (NEVER commit this!)
# Used by Vercel API routes to sign transactions
# Only set this in Vercel.  Do not set this on your local machine
# THIRDWEB_SECRET_KEY=your_secret_key_here

# -----------------------------------------------------------------------------
# Application Base URL (for on-chain storage)
# -----------------------------------------------------------------------------
# IMPORTANT: This is NOT the URL where the app is served. It's the canonical URL
# stored on-chain in dataUrl and iwpsPortalUri fields.
#
# Must point to a PERMANENT URL that will always resolve, because once stored
# on-chain, these URLs cannot be changed.
#
# Values by environment:
#   - Mainnet:  https://registry.omatrust.org
#   - Testnet:  https://test.registry.omatrust.org
#
# Priority:
#   1. Localhost chain (31337) → http://localhost:3000 (automatic)
#   2. NEXT_PUBLIC_APP_BASE_URL (if set)
#   3. Fallback → https://registry.omatrust.org
#
# NEVER set this on Preview environments - ephemeral URLs would break on-chain references.
NEXT_PUBLIC_APP_BASE_URL=https://registry.omatrust.org

# Note: NEXT_PUBLIC_VERCEL_URL (set automatically by Vercel for preview deployments)
# is used for runtime API calls but NEVER for on-chain storage.

# ============================================
# CONTRACT ADDRESS OVERRIDES (Optional)
# ============================================
# Override contract addresses for testing specific deployments
# If not set, addresses from src/config/chains.ts will be used

# NEXT_PUBLIC_REGISTRY_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
# NEXT_PUBLIC_METADATA_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512
# NEXT_PUBLIC_RESOLVER_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0

# ============================================
# DEBUG FLAGS (Optional)
# ============================================
# Enable debug logging for contract adapters
NEXT_PUBLIC_DEBUG_ADAPTER=true

# ============================================
# ERC-8004 COMPLIANCE (Optional)
# ============================================
# Use ERC-8004 compliant register() function instead of native mint()
# Default: true (spec-compliant, recommended for demos and production)
# Set to false to use the more efficient native mint() function
NEXT_PUBLIC_USE_ERC8004_REGISTER=true
