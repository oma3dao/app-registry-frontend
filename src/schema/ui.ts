/**
 * UI State Types - Part of unified data model
 *
 * This file is part of the unified data model in schema/data-model.ts
 * Kept separate for backwards compatibility during migration.
 */

// Re-export UI-related types from data-model
export type { TUIState, TFormState } from "./data-model";
export { defaultUIState, UIState, FormState } from "./data-model";

