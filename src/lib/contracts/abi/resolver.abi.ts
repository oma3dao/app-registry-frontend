/**
 * OMA3ResolverWithStore ABI
 * Contract versions are managed via Git branches
 * This branch uses the current deployed contracts
 */

import resolverAbiJson from '../../../abi/resolver.json';

export const resolverAbi = resolverAbiJson.abi as any;

