/**
 * OMA3AppRegistry ABI
 * Contract versions are managed via Git branches
 * This branch uses the current deployed contracts
 */

import appRegistryAbiJson from '../../../abi/appRegistry.json';

export const appRegistryAbi = appRegistryAbiJson.abi;
